server:
  port: ${APP_PORT:8090}

app:
  base-url: /v1/oscar
  first-service:
    base-url:
      movies: ${FIRST_SERVICE_URL:http://localhost:8080/lab2-1.0-SNAPSHOT/api}/movies
logging:
  level:
    root: info